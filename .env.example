# Anthropic API Key or OAuth token (for Claude Sonnet and Opus)
# Real API keys: https://console.anthropic.com/settings/keys
# Claude Max setup-tokens also work (set ANTHROPIC_AUTH_TYPE=bearer)
ANTHROPIC_API_KEY=

# Optional: Override Anthropic auth type (auto | api-key | bearer)
# auto = detect from key format (sk-ant-* uses api-key, else bearer)
# ANTHROPIC_AUTH_TYPE=auto

# Optional: Override Anthropic base URL (for claude-max-api-proxy)
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# Google AI API Key (for Gemini Flash)
# Get one at: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=

# Optional: Override routing mode (eco | standard | performance)
# CLAWD_THROTTLE_MODE=standard

# Optional: Override log level (debug | info | warn | error)
# CLAWD_THROTTLE_LOG_LEVEL=info

# Optional: Override config directory
# CLAWD_THROTTLE_CONFIG_DIR=~/.config/clawd-throttle
